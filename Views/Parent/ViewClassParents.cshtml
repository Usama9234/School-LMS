@model IEnumerable<School_Management_System.Models.Admin.StudentDetails>

@{
    ViewData["Title"] = "ViewClassParents";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="page-wrapper">

    <!-- Breadcrumb -->
    <div class="page-breadcrumb">
        <div class="row">
            <div class="col-12 d-flex no-block align-items-center">
                <h4 class="page-title">Parents</h4>
                <div class="ml-auto text-right">
                    <nav aria-label="breadcrumb">
                        <ol class="breadcrumb">
                            <li class="breadcrumb-item"><a asp-action="Index" asp-controller="Dashboard">Home</a></li>
                            <li class="breadcrumb-item active" aria-current="page"> View All Parents</li>
                        </ol>
                    </nav>
                </div>
            </div>
        </div>
    </div>

    <!-- Card Structure -->
    <div class="container-fluid">
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-body">

                        <!-- Table Heading -->
                        <div class="row">
                            <div class="col-md-4">
                            </div>
                        </div>

                        <!-- Table Content -->
                        <div class="table-responsive">
                            <table class="table">
                                <thead class="thead-dark">
                                    <tr>
                                        <th><label>Student Name</label> </th>
                                        <th><label>Student Roll No</label> </th>
                                        <th><label>Action</label> </th>
                                    </tr>
                                </thead>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>@item.StudentName</td>
                                        <td>@item.StudentRollNo</td>
                                        <td>
                                            <a asp-action="ViewStudentParent" asp-controller="Parent" asp-route-studentId="@item.StudentId" class="btn btn-dark">
                                                <i class="fas fa-plus-square"></i>
                                                <span>
                                                    <strong>ViewParent</strong>
                                                </span>
                                            </a>
                                        </td>
                                    </tr>
                                }
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts
{
    <script>
        $(function () {
            var successMessage = '@TempData["SuccessMessage"]';
            if (successMessage != '')
                alertify.success(successMessage);
        });
    </script>
}