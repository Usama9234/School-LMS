@model IEnumerable<School_Management_System.Models.Admin.Parent>

@{
    ViewData["Title"] = "ViewAttendance";
    Layout = "~/Views/Shared/_ParentLayout.cshtml";
}

<div class="page-wrapper">

    <!-- Breadcrumb -->
    <div class="page-breadcrumb">
        <div class="row">
            <div class="col-12 d-flex no-block align-items-center">
                <h4 class="page-title">Students</h4>
                <div class="ml-auto text-right">
                    <nav aria-label="breadcrumb">
                        <ol class="breadcrumb">
                            <li class="breadcrumb-item"><a asp-action="Index" asp-controller="ParentDashboard">Home</a></li>
                            <li class="breadcrumb-item active" aria-current="page"> View Students</li>
                        </ol>
                    </nav>
                </div>
            </div>
        </div>
    </div>

    <!-- Card Structure -->
    <div class="container-fluid">
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-body">

                        <!-- Table Heading -->
                        <div class="row">
                            <div class="col-md-4">
                            </div>
                        </div>

                        <!-- Table Content -->
                        <div class="table-responsive">
                            <table class="table">
                                <thead class="thead-dark">
                                    <tr>
                                        <th><label>Class Name</label> </th>
                                        <th><label>Student Name</label> </th>
                                        <th>Action</th>
                                    </tr>
                                </thead>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td><span>@item.ClassName</span></td>
                                        <td><span>@item.StudentName</span></td>
                                        <td>
                                            <a asp-action="ViewSubjectsAttendance" asp-controller="ParentDashboard" asp-route-classId="@item.ClassId" asp-route-studentId="@item.StudentId" class="btn btn-dark">
                                                <i class="fas fa-plus-square"></i>
                                                <span>
                                                    <strong>ViewAttendance</strong>
                                                </span>
                                            </a>
                                        </td>
                                    </tr>
                                }
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts
{
    <script>
        $(function () {
            var successMessage = '@TempData["SuccessMessage"]';
            if (successMessage != '')
                alertify.success(successMessage);
        });
    </script>
}


